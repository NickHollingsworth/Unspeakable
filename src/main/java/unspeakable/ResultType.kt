package unspeakable

/**
 * The types of results that exist. The sum of all symbols on a set of cards determine what result they give.
 * @author nick
 */
enum class ResultType {
    ADVANCE, COMPLICATION, SETBACK
}